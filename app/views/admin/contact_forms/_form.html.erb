<%= form_for(@contact_form, :url => (@contact_form.id.nil? ? admin_contact_forms_path : admin_contact_form_path(@contact_form))) do |f| %>
	<% if @contact_form.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@contact_form.errors.count, "error") %> prohibited this contact_form from being saved:</h2>
			<ul>
				<% @contact_form.errors.full_messages.each do |msg| %>
        			<li><%= msg %></li>
      			<% end %>
      		</ul>
    	</div>
  	<% end %>
  	<div class="field">
    	<%= f.label :Name %><br />
    	<%= f.text_field :Name %>
  	</div>
	<div class="field">
		<%= f.label :recipient_names %>
		<%= f.text_field :recipient_names %>
	</div>
  	<div class="field">
    	<%= f.label :Description %><br />
    	<%= f.text_area :Description %>
  	</div>
  	<div class="actions">
    	<%= f.submit %>
  	</div>
<% end %>
